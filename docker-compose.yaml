version: '3.9'

services:
  app:
    build: .
    container_name: express-app
    ports:
      - '3000:3000'
    depends_on:
      - rabbitmq

  rabbitmq:
    image: rabbitmq:3-management
    container_name: rabbitmq
    ports:
      - '5672:5672'
      - '15672:15672'